# ── Web framework ─────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]

# ── Database (Supabase PostgreSQL via asyncpg) ────────────────────────────────
sqlalchemy[asyncio]
asyncpg
alembic

# ── Task queue (Upstash Redis broker) ────────────────────────────────────────
celery[redis]
redis

# ── Data / numerics ───────────────────────────────────────────────────────────
pandas
numpy
httpx

# ── Market data: yfinance primary, Twelve Data secondary ─────────────────────
yfinance
mftool    # mutual fund NAVs only -- actively maintained v3.0
nselib    # FPI data fallback only -- v2.5.1 May 2026
# do NOT add nsepy or jugaad-trader -- both are broken/inactive

# ── Technical analysis ────────────────────────────────────────────────────────
# ta-lib requires the C library — see Dockerfile
ta-lib

# ── Machine Learning (LSTM + per-symbol models) ───────────────────────────────
tensorflow          # keras is bundled as tensorflow.keras in TF 2.x
scikit-learn
joblib

# ── NLP / sentiment (FinBERT) ─────────────────────────────────────────────────
transformers
torch
feedparser

# ── Config / env ──────────────────────────────────────────────────────────────
pydantic-settings
python-dotenv

# ── Logging ───────────────────────────────────────────────────────────────────
loguru

# ── Real-time ─────────────────────────────────────────────────────────────────
websockets

# ── LLM: Groq (fast inference) + Claude (explanations) ───────────────────────
groq
anthropic

# ── HTML parsing + BSE corporate actions ─────────────────────────────────────
beautifulsoup4
lxml
bsedata    # BSE corporate actions and announcements

# ── Rate limiting ─────────────────────────────────────────────────────────────
slowapi

# ── Zerodha Kite portfolio tracker ───────────────────────────────────────────
kiteconnect>=4.2.0   # Zerodha KiteConnect v3 — full trading + GTT + ticker
scipy                # XIRR calculation via brentq root solver

# ── Trade journal → spreadsheet ──────────────────────────────────────────────
openpyxl             # local .xlsx trade journal (SHEET_LOG_BACKEND="local")
gspread              # Google Sheets backend (SHEET_LOG_BACKEND="google")
google-auth          # Google OAuth token refresh
google-auth-oauthlib # OAuth 2.0 Desktop flow (your own account, no sheet sharing needed)
