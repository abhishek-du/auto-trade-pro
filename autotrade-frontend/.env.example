# Used by capture.cjs (puppeteer screenshot script) to log in before capturing pages
CAPTURE_EMAIL=
CAPTURE_PASSWORD=

# ── capture.cjs (puppeteer screenshot script) ────────────────────────────────
# Used only by `node capture.cjs` to log in and take dashboard screenshots.
# Leave blank unless you run that script. Never commit real values — .env is
# gitignored precisely so these stay local.
CAPTURE_EMAIL=
CAPTURE_PASSWORD=
